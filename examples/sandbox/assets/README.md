# Assets do Sandbox

Coloque aqui os assets do jogo de exemplo:
- `textures/` — arquivos .png/.jpg para SpriteRendererComponent / Material::AlbedoMap
- `audio/` — arquivos .wav/.mp3/.ogg para AudioEngine::LoadSound
- `models/` — arquivos .obj para Mesh::LoadFromOBJ / AssetManager::GetMesh

Nenhum asset binário foi incluído neste template para manter o repositório leve.
